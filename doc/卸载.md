sudo apt purge -y clawx
sudo apt autoremove -y
sudo rm -rf /opt/ClawX
sudo rm -f /usr/local/bin/clawx /usr/local/bin/openclaw
sudo rm -f /etc/apparmor.d/clawx
rm -rf ~/.openclaw ~/.config/ClawX ~/.cache/ClawX ~/.local/share/ClawX
rm -f ~/.config/autostart/clawx.desktop

which clawx
which openclaw
dpkg -l | grep clawx
ls -la /opt/ClawX
ls -la ~/.openclaw